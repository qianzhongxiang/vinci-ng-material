import { DialogModule } from './dialog.module';

describe('ConfirmModule', () => {
  let confirmModule: DialogModule;

  // beforeEach(() => {
  //   confirmModule = new DialogModule();
  // });

  // it('should create an instance', () => {
  //   expect(confirmModule).toBeTruthy();
  // });
});
